# ClearTrace — Nixpacks / Railpack build config (repo root)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix cleartrace/backend"]

[start]
cmd = "node cleartrace/backend/db/seed.js && node cleartrace/backend/server.js"
